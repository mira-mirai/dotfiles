# this file only runs for login shells. put anything here you want run only during login, and everything else in the .bashrc

# tell macos not to nag me about zsh
export BASH_SILENCE_DEPRECATION_WARNING=1

echo "loading ~/.bash_profile"
source ~/.bashrc
