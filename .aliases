# common command aliases
alias rm='rm -i'
alias la='ls -la'
alias ll='ls -l'

# git aliases
alias gs="git status" #this overrides ghostscript
alias ga="git add"
alias gc="git commit"
alias gl="git log"
alias gp="git push"

# tailscale command
alias tailscale="/Applications/Tailscale.app/Contents/MacOS/Tailscale"
